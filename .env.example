# Local development only. Never add Production values here or commit a real .env file.
NUKNUK_ENV=local
# Supabase: use a local instance or an isolated development project.
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Server/worker-only values; use test/sandbox credentials locally.
SUPABASE_SERVICE_ROLE_KEY=
OPENAI_API_KEY=

